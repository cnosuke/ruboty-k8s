require "spec_helper"

describe Ruboty::K8s do
  it "has a version number" do
    expect(Ruboty::K8s::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
